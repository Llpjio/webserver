# Port for HTTP & WebSocket server
PORT=3000

# Environment mode (development / production)
NODE_ENV=development

# Optional PostgreSQL Connection String for production (e.g. on Render)
# If left unset, E4ALL automatically uses local SQLite (data/e4all.db)
# DATABASE_URL=postgresql://user:pass@host:5432/e4all
